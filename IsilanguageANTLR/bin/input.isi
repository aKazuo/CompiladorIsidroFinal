programa

  numero a, b,c.
  texto  t1.

  leia(a).
  leia(b).
  
  escreva("baratao de cachorro").
  
  
  a := 1 + 2 * 3 / b.
  b := 2.
  c:=2+2.
  
  log(a).
  
  raiz(b).
  
  exp(c).
  
  testa (a){
  caso "bobo":{
  	escreva(a).
  	}
  caso 1: {
  	escreva(b).
  }
  }
  
  enquanto (a == "oioi"){
  escreva(a).
  }

  se ( a > b ) entao {
     escreva (a).
  }
  senao {
     escreva (b).
  }
  
  

fimprog.